@param path: String
@param message: String

@template.layout(title = "500 - Erro Interno", content = @`
    @template.partials.error(errorCode = 500, errorName = "Erro Interno", message = @`
        Erro ao processar a página que você tentou acessar: <code>${path}</code>. O erro foi: <code>${message}</code>
    `)
`)
